import React from 'react';
import { SearchContainer } from '../containers';

const SearchPage = () => {
    return (
        <div>
            <SearchContainer />
        </div>
    );
};

export default SearchPage;